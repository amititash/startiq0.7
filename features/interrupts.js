const store = require('../store/store');
module.exports = function(controller) {
    // controller.interrupts(['hi','hey','hello'], 'direct_message,message,direct_mention', async( bot, message) => {
        
    //     await bot.say({
    //         text : "Hello"
    //     })
    // })
}